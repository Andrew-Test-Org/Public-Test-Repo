# This is a basic workflow to help you get started with Actions

name: CI

# Controls when the action will run.
on:
  pull_request:

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  validate:
    runs-on: ubuntu-latest
    outputs:
      IS_DEPLOYER: ${{ steps.isFakeTeamMember.outputs.isTeamMember || github.actor == 'roryabraham' }}
    steps:
      - id: isTeamMember
        uses: tspascoal/get-user-teams-membership@baf2e6adf4c3b897bd65a7e3184305c165aec872
        with:
          GITHUB_TOKEN: ${{ secrets.RORY_TOKEN }}
          username: ${{ github.actor }}
          team: rory-custom-team

      - id: isFakeTeamMember
        uses: tspascoal/get-user-teams-membership@baf2e6adf4c3b897bd65a7e3184305c165aec872
        with:
          GITHUB_TOKEN: ${{ secrets.RORY_TOKEN }}
          username: ${{ github.actor }}
          team: fake-team

      # Should be true
      - run: echo ${{ steps.isTeamMember.outputs.isTeamMember == 'true' }}

      # Should be false
      - run: echo ${{ steps.isFakeTeamMember.outputs.isTeamMember == 'true' }}

      # Should be true
      - run: echo ${{ github.actor == 'roryabraham' }}

      # Should be false
      - run: echo ${{ github.actor == 'AndrewGable' }}

      # Should be true
      - run: echo ${{ steps.isFakeTeamMember.outputs.isTeamMember == 'true' || github.actor == 'roryabraham' }}

      # Should be true
      - run: echo ${{ github.actor == 'roryabraham' || steps.isFakeTeamMember.outputs.isTeamMember == 'true' }}

      # Should be true
      - run: echo ${{ github.actor == 'AndrewGable' || steps.isTeamMember.outputs.isTeamMember == 'true' }}

      # Should be false
      - run: echo ${{ github.actor == 'AndrewGable' || steps.isFakeTeamMember.outputs.isTeamMember == 'true' }}

      # Should be true
      - run: echo ${{ steps.isTeamMember.outputs.isTeamMember == 'true' || github.actor == 'AndrewGable' }}


  # This workflow contains a single job called "build"
  build:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest
    needs: validate

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      - run: echo ${{ needs.validate.outputs.IS_DEPLOYER }}

      - if: ${{ needs.validate.outputs.IS_DEPLOYER == 'true'}}
        run: echo "${{ github.actor }} is a deployer"

      - if: ${{ needs.validate.outputs.IS_DEPLOYER == 'false' }}
        run: echo "${{ github.actor }} is not a deployer"
