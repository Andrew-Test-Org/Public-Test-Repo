name: "Test"

on:
  workflow_dispatch:

concurrency: test-gh-concurrency

jobs:
  echo:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - shell: bash
        run: sleep 60

      - uses: Andrew-Test-Org/Public-Test-Repo/.github/actions/composite/setupNode@main
