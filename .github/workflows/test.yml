name: "Test"

on:
  workflow_dispatch:

jobs:
  echo:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - uses: Andrew-Test-Org/Public-Test-Repo/.github/actions/composite/setupNode@main
