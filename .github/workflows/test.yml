name: "Test"

on:
  push:

jobs:
  echo:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: echo ${{ toJSON('This is an issue title with \'single quotes\' and "double quotes" and also this character ^') }}
