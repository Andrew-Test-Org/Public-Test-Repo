name: 'Test Nested Composite Action'
on:
  push:
    branches: [Rory-NestedCompoundAction]
jobs:
  main:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - uses: ./.github/actions/composite/myCompositeAction@Rory-NestedCompoundAction
